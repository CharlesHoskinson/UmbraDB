{"verdict":"PASS","summary":"At HEAD e744d994fc94d7770fbd2c802d7bd4480cce83db, the default production sync paths have a real independent-freshness gap: all three wallets declare completion relative to indexer-supplied event/transaction maxima, with no node-sourced finalized-head reconciliation. This is worth filing publicly as a sync-semantics/hardening request, not as a fund-loss vulnerability: the canonical specification explicitly places the indexer inside the trusted model. The claim should not say a “deeper re-sync” is necessarily required—an honest lagging indexer’s live stream should eventually catch up, while re-syncing against the same malicious source does not solve the problem.","findings":[{"severity":"medium","file":"packages/abstractions/src/SyncProgress.ts","title":"Shielded completion is equality with an indexer-derived event maximum","detail":"The exact predicate at packages/abstractions/src/SyncProgress.ts:31-33 is `const applyLag = BigInt(Math.abs(Number(data.highestRelevantWalletIndex - data.appliedIndex))); return data.isConnected && applyLag <= maxGap;`. `isStrictlyComplete()` passes `0n` at lines 65-66, and ShieldedWallet.waitForSyncedState defaults to zero at packages/shielded-wallet/src/ShieldedWallet.ts:310-313. The supposed tip is assigned directly from `BigInt(lastUpdate.maxId)` at packages/shielded-wallet/src/v1/Sync.ts:292-306. Thus strict shielded completion means applied event ID equals the indexer's `maxId`, not that the SDK has reached an independently verified chain head.","blocking":false},{"severity":"medium","file":"packages/dust-wallet/src/v1/Sync.ts","title":"Dust uses the same indexer-relative completion criterion","detail":"Dust uses the shared predicate in packages/abstractions/src/SyncProgress.ts:31-33 and strict zero-gap behavior at lines 65-66; packages/dust-wallet/src/DustWallet.ts:424-427 defaults waitForSyncedState to zero. Its sync capability takes `updates.at(-1)!.maxId` as `highestRelevantWalletIndex` at packages/dust-wallet/src/v1/Sync.ts:327-345. The Dust GraphQL schema describes `maxId` only as “The maximum ID of all dust ledger events” at packages/indexer-client/indexer.gql:482-484. It is not independently authenticated freshness data.","blocking":false},{"severity":"medium","file":"packages/unshielded-wallet/src/v1/SyncProgress.ts","title":"Unshielded completion trusts the indexer's address-specific progress sentinel","detail":"The exact predicate at packages/unshielded-wallet/src/v1/SyncProgress.ts:29-31 is `const applyLag = BigInt(Math.abs(Number(data.highestTransactionId - data.appliedId))); return data.isConnected && applyLag <= maxGap;`; strict completion passes `0n` at lines 53-54. packages/unshielded-wallet/src/UnshieldedWallet.ts:341-344 uses that predicate with a default zero gap. The subscription requests `highestTransactionId` at packages/indexer-client/src/graphql/subscriptions/UnshieldedTransactions.ts:67-70, and packages/unshielded-wallet/src/v1/Sync.ts:109-114 copies it directly into progress. The schema explicitly defines it as the highest transaction ID “currently known” for the address at packages/indexer-client/indexer.gql:1398-1405, so an incomplete indexer's own knowledge is the reference point.","blocking":false},{"severity":"medium","file":"packages/shielded-wallet/src/v1/Sync.ts","title":"No node/consensus/finality reconciliation exists in the default sync path","detail":"The three DefaultSyncConfiguration types accept only indexer connectivity for sync (packages/shielded-wallet/src/v1/Sync.ts:43-79, packages/dust-wallet/src/v1/Sync.ts:75-112, packages/unshielded-wallet/src/v1/Sync.ts:38-46). A production-source search found no `grandpa`, `getFinalizedHead`, `chain_getHeader`, or `chain_getFinalizedHead` in any wallet sync path. Dust does query a latest block, but packages/dust-wallet/src/v1/Sync.ts:207-229 obtains it from the same indexer, and packages/capabilities/src/validation/blockData.ts:29-49 explicitly calls its source “the latest block from indexer.” The node client’s finality handling at packages/node-client/src/effect/PolkadotNodeClient.ts:223-245 is transaction-submission status only; it is wired through packages/capabilities/src/submission/submissionService.ts:72-108 and does not gate SyncProgress. Therefore there is no existing mitigation by an independent finalized tip.","blocking":false},{"severity":"low","file":"docs/spec/Specification.md","title":"The indexer is explicitly trusted, which blocks vulnerability framing","detail":"The canonical specification says node-fed synchronization is best for security/privacy at docs/spec/Specification.md:740-743, then describes indexing as an alternative “at the cost of having to trust said service” at lines 745-748. For shielded indexing it repeats that the service “needs to be trusted by the user” at lines 752-761. Consequently, a malicious or compromised indexer is outside the documented trust boundary: this is a hardening/design limitation, not an untrusted-indexer security bug. Also, an honest lagging indexer can later push new events over the existing live subscription; a deeper re-sync is not inherently necessary, and a deeper re-sync against the same malicious indexer is ineffective. This blocks publishing the original claim verbatim, but not filing the narrowed hardening issue.","blocking":true},{"severity":"medium","file":"packages/facade/src/index.ts","title":"Impact is stale view integrity and availability, not direct fund loss","detail":"FacadeState.isSynced simply ANDs the three strict predicates at packages/facade/src/index.ts:287-292, and waitForSyncedState delegates to those wallets at lines 751-759. A stale prefix can therefore be labeled synced and omit incoming funds, delay recognition of finalized spends, show stale balances/UTXOs, or cause attempts to spend already-consumed inputs. It does not give the indexer signing/spending keys or bypass ledger validation; conflicting transactions should be rejected by the node. The repository itself separates “submitting transactions to a node” from “syncing state with indexer” at README.md:8-10. Classify the direct impact as view-integrity/availability and transaction failure/UX risk, not unauthorized fund transfer.","blocking":false},{"severity":"medium","file":"packages/facade/src/index.ts","title":"Minimal correct fix requires an independently anchored completeness signal","detail":"For honest-staleness detection, add a progress anchor containing the indexer's fully processed finalized block hash/height, independently obtain the finalized head and headers from a configured node, verify the anchor is canonical/finalized and within a defined lag, and require both that check and cursor completion before exposing chain-synced status. Preserve or rename the current result as indexer-synced. Against a malicious indexer, merely comparing its claimed height/hash to the node head is insufficient because it can copy the head while omitting records. Closing that stronger threat requires the supplied events/UTXO result and completeness to be cryptographically bound to finalized node state—such as verifiable state/index proofs—or replaying/scanning finalized node data locally. Add contiguous-cursor checks as defense in depth, but they alone cannot detect a consistently truncated prefix.","blocking":false},{"severity":"low","file":"SECURITY.md","title":"Public filing is appropriate only with hardening language","detail":"SECURITY.md:14-19 directs suspected security vulnerabilities to private vulnerability reporting rather than public issues. Because the documented model trusts the indexer, a public issue is reasonable if framed as independent freshness hardening, clearer `isSynced` semantics, and resilience to lag/stale service data. If it is instead presented as an exploitable malicious-indexer vulnerability, follow the repository's private-reporting policy.","blocking":false}]}